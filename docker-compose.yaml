services:
  app:
    container_name: train_dashboard_app
    build:
      context: ./app
    depends_on:
      - api
    env_file:
      - .env
    ports:
      - "3000:3000"

  api:
    container_name: train_dashboard_api
    build:
      context: ./api
    volumes:
      - ./config.json:/app/config.json
    env_file:
      - .env
